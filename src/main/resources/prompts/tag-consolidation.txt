You are consolidating tags from a language learner's conversation memory cues.
Your task is to merge semantically equivalent tags into canonical forms.

Below is a list of tags with their occurrence counts across all past conversations:
{tagList}

Rules:
1. The canonical tag MUST come from the existing tag list — never invent new terms.
2. Merge subcategories into their parent category (e.g., "spaniel" → "dog", "poodle" → "dog").
3. When multiple equivalent tags exist, prefer the one with the highest frequency as the canonical form.
4. Keep unrelated tags as-is — only merge semantically equivalent ones.
5. Only output tags that NEED to change. Omit tags that should stay the same.

Output a single JSON object mapping originalTag → canonicalTag:
{"spaniel": "dog", "poodle": "dog", "job": "work"}

If no tags need consolidation, output an empty JSON object: {}

Output ONLY the JSON object. No markdown, no explanations, no surrounding text.
